# Status

## 현재 단계

`울트라돌맹의포스트잇`이라는 사용자 표시 이름과 사진 앱 아이콘을 적용했고, 좌우 커서 호출형 공용 인덱스 트레이와 엣지별 3칸 ICE 스택에 TextKit 2 네이티브 편집기를 연결했습니다.

## 구현된 제품 동작

- 메뉴 막대 상주, Dock 미표시, 모든 Space 및 전체 화면 보조 패널
- 왼쪽·오른쪽 엣지의 2px 핫존, 상단 엣지 제거
- 엣지 첫 진입에서 즉시 표시되고 포인터가 떠나도 유지되며, 같은 엣지 재진입에서 숨는 토글형 인덱스
- 반대 엣지나 다른 화면 엣지 진입 시 기존 트레이를 먼저 숨기고 새 커서 높이에서 나타나는 전환
- 어느 쪽에서 호출해도 동일한 전역 순서의 공용 인덱스 트레이
- ICE 시 양쪽 공용 트레이에서 인덱스가 빠지고, 닫으면 전역 순서로 복귀
- 좌우 엣지의 26px 높이 가로형 제목 탭, hover·pressed·selected 피드백과 즉시 본문 포커스되는 `ICE` click
- 공용 트레이 옆 `+`를 통한 현재 호출 엣지의 새 임시 메모 생성
- 호버 미리보기를 제거해 인덱스 드래그와 휴지통 이동을 방해하지 않음
- 본문과 분리된 최대 40자 제목 입력줄, 기존 6글자 파생 제목의 조건부 전체 복원
- 엣지별 세로 ICE 레인 최대 3개, 1·2개는 클릭 위치를 우선한 자유 배치, 충돌 시 기존 메모 우선 이동과 새 메모 최소 이동 fallback
- 화면 3분의 1 고정 높이, 세 번째 레인에서만 3영역 정렬, 네 번째에서 가장 오래된 레인 FIFO 복귀
- 가로 레인 양 끝의 바깥 `+`와 메모 사이 hover 시 8px→36px로 부드럽게 벌어지는 삽입 `+`, 정확한 중간 순서 삽입
- 최초 mother 메모를 기준으로 레인 전체가 하나의 공통 색을 사용하고, 어느 구성원에서 색을 바꿔도 전체와 이후 삽입 메모에 함께 반영
- 가로 레인 구성원 중 하나에서 `Esc` 또는 `Cmd+W`를 누르면 전체 레인이 함께 접혀 공용 인덱스로 복귀
- 트레이 안 전역 순서 변경만 허용하는 인덱스 드래그, 유효 영역 밖 드롭 시 원래 트레이로 안전 복귀
- 인덱스 드래그 중 하단 삭제 영역 표시, 드롭 확인 후 본문과 첨부파일 영구 삭제
- 작은 화면 전역 압축과 남은 연속 공간별 추가 압축
- 여러 개의 독립 ICE 패널과 별도 `hidden / visible(edge) / dragging` 인덱스 상태, 좌우 엣지 전환 전 기존 창을 숨겨 순간이동 제거
- 기본 동적 화면 모드에서 모든 연결 모니터의 좌우 핫존 생성, 화면 분리 시 해당 pointer 상태 즉시 정리
- ICE 레인은 열린 디스플레이에 고정하고, 분리된 디스플레이의 레인만 사용 가능한 화면으로 fallback
- 메모 수 제한과 활성·보관 구분을 없앤 단일 공용 인덱스, 드래그 확인을 통한 명시적 영구 삭제
- 임시 빈 초안 비저장, 기존 메모가 비면 해당 패널 접기/종료 시 영구 삭제
- 첨부파일 staged 삭제와 다음 실행 복구 또는 마무리
- 저장 실패 시 최신 본문을 메모리에 보존하고 접기·FIFO 교체·종료를 막은 뒤 재시도 또는 원문 복귀로 안전 해제
- Carbon `Cmd+Shift+M`, 열린 패널의 순환과 숫자 단축키

## 편집기

- `swift-markdown-engine` `0.10.0`을 정확히 고정한 AppKit TextKit 2 편집기입니다.
- `WKWebView`, Milkdown/Crepe, npm 의존성과 번들 리소스를 제거했습니다.
- Markdown 원문은 그대로 저장하고 커서가 벗어난 서식 마커만 시각적으로 숨깁니다.
- SwiftUI 한 줄 TopBar는 ICE에서 항상 보이며 아이콘·구분선·활성 상태가 메모 글자색을 따릅니다.
- 최소 폭에서도 스크롤 없이 제목·굵게·기울임·목록·이미지가 고정되고, 나머지 명령은 이름이 보이는 `더보기` 메뉴에서 접근합니다.
- 서식 버튼은 30pt 클릭 영역과 hover·pressed·active 피드백, 한국어 접근성 이름을 가집니다.
- 제목과 본문, TopBar와 스크롤 영역은 각각 명시적으로 분리돼 서로 겹치지 않습니다.
- 굵게, 기울임, 취소선, 제목, 목록, 인용, 인라인 코드, 코드 블록, 이미지 명령을 네이티브 버스로 연결했습니다.
- 체크리스트, GFM 표, HTML 붙여넣기 변환, 맞춤법과 macOS Writing Tools를 지원합니다.
- 본문 기본 글꼴 11pt와 기존 메모별 글자색·여백 설정을 유지합니다.
- `# + Space` 입력, 한글 문자열, 굵게 명령, 숨겨진 마커, 실행 취소 범위를 TextKit 실체로 테스트합니다.
- 로컬 이미지는 20MB 제한과 실제 이미지 검증을 거쳐 안전한 전용 경로 해석기로 표시합니다.
- 엔진의 개발용 `MD_PERF` 타이핑 로그는 앱 시작 시 비활성화합니다.

## UI/UX 마감과 접근성

- `EdgeMotionPolicy`가 인덱스·ICE·컨트롤·삭제 영역·재배치의 시간과 접근성 동작을 한곳에서 결정합니다.
- ICE 색상·제목 셸은 기하 애니메이션 내내 유지하고 TextKit 본문만 유효한 전체 프레임 뒤에 mount합니다.
- 동작 줄이기에서는 창의 위치·크기 보간을 제거하고 최대 80ms의 짧은 페이드만 사용합니다.
- 투명도 줄이기와 대비 증가 설정을 인덱스, ICE, 삭제 영역, 제목 표시줄과 구분선에 반영합니다.
- 보관함 창·메뉴·상태·10개 제한을 제거해 생성부터 편집까지 단일 인덱스 흐름만 남겼습니다.
- 드래그 삭제 확인은 취소를 기본 동작으로 두고 영구 삭제를 명시적 파괴 동작으로 표시합니다.
- 설정은 메모 삭제 뒤에도 선택이 유효하게 유지되고, 32pt 색상 칩과 선택 표시 및 접근성 상태를 제공합니다.

## 저장과 이전

- `schemaVersion: 5` 문서는 `notes`, 엣지별 단일 `edgeGroups`, `defaultGroupID`를 가지며 `isActive`는 저장하지 않습니다.
- 메모 배치는 `placement.groupID`와 `placement.order`로 저장됩니다.
- `panelSize.width`만 사용자 조절값으로 사용하며 높이·위치·열림 상태는 자동 배치 상태라 저장하지 않습니다.
- v4 원본은 `notes-pre-no-library-v5-<timestamp>.json`으로 백업하고 모든 과거 보관 메모를 공용 인덱스로 복원합니다.
- v3 원본은 `notes-pre-edge-stack-v5-<timestamp>.json`으로 백업하고 엣지·순서·폭을 보존해 자유 그룹을 평탄화합니다.
- 새 메모 색은 검정을 제외한 6색 팔레트에서 무작위로 선택하고 최근 메모와 같은 색은 피합니다.
- v2 이전 직전에 원본 바이트를 `notes-pre-edge-v3-<timestamp>.json`으로 백업합니다.
- v2 활성 메모는 `handlePosition` 내림차순으로 정렬해 오른쪽 중앙 기본 그룹에 합칩니다.
- 빈 legacy 메모는 백업에만 남고 라이브 v5 데이터에는 들어오지 않습니다.
- 백업 또는 저장 실패 시 원본 v2를 유지하고 workspace를 열지 않습니다.
- `MEMODOLMAENG_DATA_DIR`로 실제 데이터와 분리된 실행 검증이 가능합니다.
- `dist/`에는 실제 본체인 `울트라돌맹의포스트잇.app` 하나만 유지합니다. 패키징용 실행 파일은 iCloud 작업 폴더의 `.build`를 쓰지 않고 외부 임시 scratch에서 매번 새로 링크합니다. 검증한 서명 번들로 `dist` 앱 폴더의 내부만 갱신해 Finder 직접 실행과 중복 방지를 둘 다 유지합니다. iCloud가 예전 이름의 앱을 늦게 복원하는 경우에도 다음 빌드가 구버전만 iCloud 휴지통으로 보냅니다.

## 실제 데이터 이전 결과

- 실제 v2 메모 9개를 v3로 이전했고 ID, 제목, 본문, 활성 상태와 표시 순서가 모두 일치합니다.
- 원본 바이트 백업은 `Backups/notes-pre-edge-v3-20260715-050947-366.json`입니다.
- 이전 전 원본과 백업의 SHA-256은 모두 `25596e6c3a8131280ebad418ffc4ec6a58f2a71bc794e7263c4c2d57ff388e11`입니다.
- 실제 10개 메모를 v4로 이전해 왼쪽·오른쪽 엣지별 단일 그룹으로 평탄화했습니다. ID·제목·본문·활성 상태는 이전 전후 동일합니다.
- v3 원본 백업은 `Backups/notes-pre-edge-stack-v4-20260716-063517-971.json`이며 SHA-256은 `bbfb4a5ee2f50cfed0c62f93335cfbaea6ec276c21a1cbcdd4657b361845211f`입니다.
- 앱을 다시 실행해도 v3 백업은 중복 생성되지 않았고 9개 본문도 변하지 않았습니다.

## 자동 검증 기준선

- Swift 테스트: 82개 통과
- TextKit 2 통합 테스트: `# + Space`, 한글 왕복, 굵게 명령, 마커 숨김, 스크롤바 제거, 로컬 이미지와 경로 탈출 차단
- v1/v2/v3/v4→v5 백업과 rollback, 기존 보관 메모의 단일 목록 복원, 25개 연속 생성, 빈 메모 필터와 그룹 정규화
- 좌우 공용 엣지 토글 트레이, 고정 높이 1·2개 충돌 회피, 3영역 정렬, 네 번째 레인의 FIFO 자동 복귀, 화면 fallback
- 좌우·중간 인접 초안의 mother 색상 공유·가로 삽입 순서·공통 세로 위치·빈 초안 비저장·그룹 접기·트레이 신규 생성 격리 회귀 테스트
- staged 첨부파일 삭제의 재실행 복구와 마무리, 업로드 위장 파일과 asset path traversal 차단
- 두 번째 ICE가 첫 ICE와 저장 데이터를 유지하고, 한 ICE만 접히며, 편집 명령이 문서별로 격리되는 회귀 테스트
- 빠른 열기→닫기→열기 뒤 단일 패널 수렴, 네 번째 ICE의 최종 3패널 무겹침, 열기·접기 전후 저장 파일 바이트 동일성
- 16ms 간격 창 프레임 샘플에서 열기·접기 크기가 역행하지 않고, 동작 줄이기 정책이 기하 이동을 제거하는 회귀 테스트
- 실제 포커스와 FIFO 순서 분리, 저장 실패 뒤 원문으로 Undo할 때 종료 잠금 해제, 화면 분리 시 핫존 pointer 정리, 이미지 20MB 경계값 회귀 테스트

## 검증 완료와 남은 현장 확인

- 실제 9개 메모 복사본 이전, 본문·ID·순서 비교, 재실행 복원을 완료했습니다.
- 기존 통합 표면 검증은 완료됐고, v4에서는 ICE를 별도 자동 배치 패널로 분리했습니다.
- 실제 키 이벤트로 `# + Space`와 한글 입력을 저장하고 인덱스를 다시 눌러 접는 흐름을 확인했습니다.
- 현재 사용자 메모 9개 복사본을 로드한 뒤 새 메모만 추가됐고, 기존 9개 객체는 원본과 동일했습니다.
- 격리 데이터에서 패널 폭을 340px에서 460px로 직접 드래그해 저장했고, 앱 재시작 후 460×454 크기가 그대로 복원되는 것을 확인했습니다.
- 격리 데이터에서 접힘 40/200/450ms 시점의 패널·인덱스 상태를 확인했습니다. 패널은 선택 인덱스에서 펼쳐지고, 다시 인덱스로 접힌 뒤 둘이 함께 숨습니다.
- 격리된 실제 9개 메모 복사본에서 서로 다른 두 세로 ICE 레인은 독립 `NSPanel`로 유지되고, 같은 가로 레인은 `Cmd+W` 또는 `Esc` 한 번에 함께 접히도록 분리해 검증했습니다. 전후 저장 파일 SHA-256은 동일했습니다.
- 현재 10개 메모 복사본에서도 두 ICE를 동시에 열고 한 패널만 접었으며, 자동 열기·접기 뒤 저장 파일 SHA-256이 실제 원본과 동일함을 확인했습니다.
- 격리된 기존 9개 메모에 6색 팔레트를 분산 적용해 검정 단일색을 제거했고, 밝은 배경에는 어두운 글자색을 적용했습니다.
- 웹 편집기 리소스를 제거한 최종 TextKit 2 패키지를 실제 데이터로 실행했고, 기존 메모 파일 해시와 9개 메모가 바뀌지 않았습니다.
- 최종 앱 번들에는 네이티브 실행 파일, `Info.plist`, 사진 기반 `AppIcon.icns`만 포함되며 Milkdown, Crepe, KaTeX 번들은 남아 있지 않습니다.
- 실제 포인터를 좌우 2px 핫존의 서로 다른 높이에 보내 공용 트레이가 커서 근처에 나타나는 것을 캡처했습니다. 한쪽에서 ICE로 연 메모가 반대쪽 트레이에서도 빠지고, 상단 핫존 패널이 생성되지 않는 것도 윈도우 목록으로 확인했습니다.
- 2026-07-18 격리 데이터에서 mother→child→grandchild 연속 생성을 실행해 두 파생 메모가 같은 색을 상속하고, 펼침 직후 첫 입력이 각각 자기 메모에만 저장되는 것을 확인했습니다. 실제 사용자 `notes.json`은 사용하지 않았습니다.
- 2026-07-18 시작 복구 경고의 `NoteStoreError error 0`은 지원하지 않는 스키마 버전 케이스였고, 이전 패키지 실행 파일이 v5 데이터를 읽지 못한 상태였다는 것이 가장 강한 원인 근거입니다. 실제 5개 메모 원본의 SHA-256 `7794963dd512acef40d187a15fb41d2937fbcf5d73ace1088463b22ad74c5c6d`는 실행 전후 동일했고, 외부 scratch에서 새로 빌드한 본체는 복구 경고 없이 시작했습니다. 진단 전 복사본은 `Backups/notes-before-startup-diagnosis-20260718-214001.json`에 남겼습니다.
- 일반 앱 동작만 수행한 구간의 AppKit 음수 geometry 로그는 0건이었습니다. Computer Use가 SwiftUI `Table` 접근성 트리를 수집할 때만 6건이 재현되어 자동화 관찰 부작용으로 분리했습니다.
- 로컬 번들은 ad-hoc 서명 후 strict 검증을 통과합니다. 실제 다중 모니터 분리·재연결, 서로 다른 배율, 전체 화면·여러 Space와 동작 줄이기 상태의 시각적 연속성은 해당 장비 상태에서 현장 확인이 남아 있습니다.
